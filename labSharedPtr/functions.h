#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <vector>
#include <string>

// -----------  функции для main.cpp

bool isVowelLetter(char);
std::vector<char*> splitWord(std::string);
char* MedianaFind(std::vector <char*>);
char* thirtiethPerz(std::vector <char*>);
char* seventiethPerz(std::vector <char*>);

#endif

